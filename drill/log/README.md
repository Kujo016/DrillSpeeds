# Drill Reports

This directory contains generated human-readable drill-speed reports.

Report names are based on target bit size, material, and density. When a name already exists, the writer appends a numeric suffix instead of replacing the existing report.

Reports contain the selected inputs and calculated RPM, horizontal speed, vertical speed, and steps per drill-bit diameter.
